# spring.datasource.url=jdbc:h2:mem:manapani
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=root
# spring.datasource.password=Nagatarun,5
# spring.h2.console.enabled=true
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# --- PostgreSQL Configuration (for Render deployment) ---
# Use the single DATABASE_URL provided by the Render environment
spring.datasource.url=${DATABASE_URL}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# The username and password will be parsed from the URL by Spring Boot

spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true

jwt.secret=your-super-secret-key-that-is-long-enough-for-hs256

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=manaa.pani.45@gmail.com
spring.mail.password=ldbbmyllwpsdxhcv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
